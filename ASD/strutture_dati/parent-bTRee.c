#include "headers/parent-bTRee.h"

/*
 * Sia dato un albero T (non vuoto), rappresentato con puntatori child e sibling, nel quale ogni
 * nodo è etichettato con un numero intero. Si dia un algoritmo che restituisca true se l’etichetta di ogni nodo
 * che ha almeno un figlio è uguale alla somma delle etichette dei figli, e false altrimenti.
 */
bool nodeEqualsToChildSum(btree t)
{
    if(t == NULL) return true;

    
}

int main()
{
    return 0;
}