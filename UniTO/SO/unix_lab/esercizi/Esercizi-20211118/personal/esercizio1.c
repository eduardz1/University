#include <std.io>

// inserisci l'upper bound da tastiera, genera un numero rand() tra 0 e questo bound, se l'utente da tastiera riesce a indovinare il numero il prog finisce, se è minore ti dice che è minore, same per maggiore, se non riesce ad indovinare in tempo secondi (signal) specificati da tastiera il programma finisce con errore 
int main(int arc, char *argv[]){
	// completa altrimenti guarda lo 09
	
